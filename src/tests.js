export const genererTests = (code) => {
    // Logique pour générer des cas de test
    console.log('Génération de scénarios de tests...');
    const tests = []; // Logique pour créer des tests
    return tests;
};