export const analyserCode = (code) => {
    // Logique d'analyse statique du code
    console.log('Analyse du code...');
    const problèmes = []; // Logique pour identifier les problèmes
    return problèmes;
};